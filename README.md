
# uz_meh_raf_website

A blog website for me and my friends (Mehrab and Rafay).

## Updates

Versioning is done using "npm version [new-version] --git-tag-version false" 
Updates follow this format: [major change].[minor change].[bug fixes].

update 0.6.1:
- Added a guide for the writers to understand how to use md files.

update 0.6.0:
- In the update table, if `order.txt` changes, the table says "ordering" instead of "modified".

update 0.5.1:
- Improved error handling.

update 0.5.0:
- Website now uses Roboto font.
- Error message is display to user when fetch error occurs.
- File on jsDelivr is purged when there is a fetch error.
- `navLinks` is now [] instead of [""].
- reactStrictMode is now used.

update 0.4.0:
- Using Supabase storage instead of Vercel Blob for storing recent commits.

update 0.3.0:
- Website now uses Vercel Blob to store recents commits (stores for 100000 ms).

update 0.2.1 - 0.2.2:
- Added default value for fetch in home page (in case of fail).

update 0.2.0:
- Added update table which shows last 5 commits.

update 0.1.4:
- Markdown now safely converts.

update 0.1.0 - 0.1.3:
- Testing how to use the content repo with this website.